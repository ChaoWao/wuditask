interface:
  display_name: "Release WudiTask Run"
  short_description: "Stop one exact active agent run safely"
  default_prompt: "Use $wuditask-release to stop this exact login and run_id without changing GitHub ownership."
