interface:
  display_name: "List WudiTasks"
  short_description: "List queue owners and active agents safely"
  default_prompt: "Use $wuditask-list to list relevant entries in the shared WudiTask queue."
